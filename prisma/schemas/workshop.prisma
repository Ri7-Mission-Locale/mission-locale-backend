model Workshop {
    id        Int       @id @default(autoincrement())
    title     String
    content   String?
    startDate DateTime
    endDate   DateTime
    createdAt DateTime  @default(now())
    updatedAt DateTime  @updatedAt
    tags  Tag[]


   
}