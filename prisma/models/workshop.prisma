/**
 * Table des ateliers
 */
model Workshop {
    workshop_id Int     @id @unique @default(autoincrement())
    title       String
    description String
    events      Event[] // Pour la repetition des ateliers
    tag         Tag[]   @relation("workshop_tags")
}
