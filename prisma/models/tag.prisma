/**
 * Mots clefs/Themes des ateliers (Unused)
 */
model Tag {
    tag_name String     @id @unique
    color    String     @unique
    workshop Workshop[] @relation("workshop_tags")
    articles Article[]  @relation("article_tags")
}
