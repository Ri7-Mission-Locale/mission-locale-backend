/**
 * Mots clefs/Themes des ateliers
 */
model Tag {
    tag_name String     @id @unique
    color    String     @unique
    workshop Workshop[] @relation("workshop_tags")
    news    News[]  @relation("news_tags")
}
